const PUBLIC = "public/";
const SCRIPTS = PUBLIC + "js/";
const STYLES = PUBLIC + "css/";

const config = {
    "routes": {
        "public": PUBLIC,
        "scripts": SCRIPTS,
        "styles": STYLES
    }
}

module.exports = config;